START

Define the general rule:
    IF X is a bird
        THEN X can fly

Define exceptions:
    Penguin cannot fly
    Ostrich cannot fly

Add bird facts

Check whether the animal is a bird

IF animal is a bird THEN
    IF animal is an exception THEN
        Animal cannot fly
    ELSE
        Animal can fly
    END IF
ELSE
    Animal is not a bird
END IF

Display the result

STOP